# AI_PROMPTS.md

Список промптов, которые использовались в процессе работы с AI (ChatGPT / Claude) над проектом «Книжный магазин».

---

## 1. Code Review (Рецензирование кода)

> Проведи code review для этой Django view-функции `order_create`. Найди проблемы с обработкой ошибок, валидацией входных данных и потенциальные баги. Предложи исправленный вариант кода.

> Проведи code review для функции `payment_process` (интеграция со Stripe Checkout). Обрати внимание на обработку ошибок Stripe API, повторную оплату уже оплаченных заказов и логику работы с пустыми заказами.

> Проведи code review для `step2` — второго шага формы добавления книги без использования стандартных Django Forms. Найди проблемы с валидацией `category_id`, количества на складе (`stock`) и потерей введенных данных формы при возникновении ошибки.

---

## 2. Генерация тестов

> Сгенерируй unit-тесты Django (на базе TestCase) для моделей `Category`, `Book`, `Order`, `OrderItem` из файла models.py. Покрой тестами: создание объектов, строковое представление `__str__`, обязательные поля, методы `get_total_cost()` и `get_cost()`, значения по умолчанию (`stock`, `paid`) и связи ForeignKey.

> Добавь к каждому тесту обязательный комментарий `# Generated with AI, reviewed and modified` на русском языке и убедись, что тесты изолированы и не зависят друг от друга (используй метод `setUp`).

---

## 3. Документация

> Сгенерируй docstrings на русском языке для всех контроллеров в файле views.py — опиши назначение каждой функции/класса, принимаемые параметры и возвращаемое значение.

> Обнови файл README.md проекта: добавь подробное описание функционала (каталог книг, корзина, оформление заказа, оплата через Stripe, регистрация/авторизация, пошаговый мастер добавления книги) и пошаговую инструкцию по запуску.

> Добавить в README раздел "AI Usage", описывающий, какие именно части проекта создавались или проверялись с помощью искусственного интеллекта, и добавь ссылки на отчеты AI_REVIEW.md и AI_PROMPTS.md.

---

## Примечание

Все рекомендации AI были тщательно проверены вручную перед интеграцией в проект. Часть предложений (например, полный переход на Django ModelForm в функции `step2`) была признана избыточной для текущего объема учебного задания и отклонена — соответствующая пометка с обоснованием оставлена в файле AI_REVIEW.md.