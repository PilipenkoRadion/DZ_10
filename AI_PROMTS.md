# AI_PROMPTS.md

Список промптів, які використовувались під час роботи з AI (Claude, Anthropic) над проєктом "Книжковий магазин".

## 1. Code Review

> Проведи code review для цієї Django view-функції `order_create`. Знайди проблеми з обробкою помилок, валідацією вхідних даних та потенційні баги. Запропонуй виправлений код.

> Проведи code review для `payment_process` (інтеграція зі Stripe Checkout). Зверни увагу на обробку помилок Stripe API, повторну оплату вже оплачених замовлень та порожні замовлення.

> Проведи code review для `step2` — другого кроку форми додавання книги без використання Django Forms. Знайди проблеми з валідацією `category_id`, `stock`, втратою даних форми при помилці.

## 2. Генерація тестів

> Згенеруй unit-тести Django (pytest/TestCase) для моделей `Category`, `Book`, `Order`, `OrderItem` з файлу models.py. Покрий: створення об'єктів, `__str__`, обов'язкові поля, метод `get_total_cost()` та `get_cost()`, значення за замовчуванням (`stock`, `paid`), зв'язки ForeignKey.

> Додай до кожного тесту коментар "Generated with AI, reviewed and modified" та переконайся, що тести не залежать один від одного (використовуй setUp).

## 3. Документація

> Згенеруй docstrings українською/російською мовою для всіх views у файлі views.py — опиши призначення кожної функції/класу, параметри та що вона повертає.

> Онови README.md проєкту: додай опис функціоналу (каталог книг, кошик, оформлення замовлення, оплата через Stripe, реєстрація/логін, багатокроковий майстер додавання книги) та інструкцію із запуску.

> Додай в README секцію "AI Usage", що описує, які саме частини проєкту створювались/перевірялись за допомогою AI, і додай посилання на AI_REVIEW.md та AI_PROMPTS.md.

## Примітка

Усі рекомендації AI були перевірені вручну перед застосуванням. Частина пропозицій (наприклад, повний перехід на Django ModelForm у `step2`) визнана надлишковою для обсягу завдання і не застосовувалась — залишено відповідну примітку в AI_REVIEW.md.